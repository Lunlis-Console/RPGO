# Архитектурный аудит — LostAndDivine (22.08.2026)

Статус: read-only исследование + решенные инварианты заказчика 22.08. Код не менялся после 21.08, билд не запускался.
Стек: net8.0, Shared + Server (SQLite game.db/content.db, FluentMigrator) + ClientMonoGame + Editor.Wpf + Editor.Core.
Метод: Read/Glob/Grep + 4 explore-агента (Server/Shared/Client/Build), ключевые факты перепроверены вручную по файлам.

---

## 0. Решения заказчика 22.08 (заморозка инвариантов для рефактора)

| # | Вопрос | Ответ 22.08 | Влияние на приоритеты |
|---|---|---|---|
| 1 | Целевая CCU | **2000** | P1-DB (WAL/busy_timeout, unbounded queue) и P1-Net (BroadcastMap O(P×E), лимит) — в Фазу 0-1, без оптимизации 2000 = OOM/GC-спайки |
| 2 | Совместимость сейвов | **Можно ломать (breaking-change)** | Можно вводить value-objects `Health/Level/Gold/Quantity`, миграции с конвертацией, не тянуть скотч для старых сейвов |
| 3 | Заморозка фич | **Да, чиним стол 2-3 недели** | Фазы 0-1 идут без новых фич, не распыляемся между стабильностью и фичами |
| 4 | content.db источник правды | **Только `content.editor.db`**, кнопка `Опубликовать` уже есть | Staging уже реализован `Editor.Core/Db.cs:43-46,85-135` — добить атомарность/WAL, live-мутаций нет |
| 5 | Подписанный инсталлер | **Да, у игроков есть печать** | P1-9 ретро-fit возможен: `build-client-build.ps1:175-199` подпись уже есть, форсим `minVersion` floor без нового инсталлера |
| 6 | Платформы клиента | **Только Windows** | `SpriteCache.cs:46` без кроссплатформы, достаточно Dispose лимит + eviction |

Вывод: при CCU 2000 + разрешенном breaking-change + заморозке — стоимость фикса сейчас минимальна, дальше растет линейно с контентом.

---

## 1. Дельта к аудиту 21.08 — что уже закрыто (не трогать повторно)

| Проблема 21.08 | Статус 22.08 | Доказательство |
|---|---|---|
| P0-1 `DropAllTables` при потере `VersionInfo` молча вайпал | **Частично закрыто** — гейт `LAD_ALLOW_DB_RESET=1` + `VACUUM INTO` бэкап | `Shared/Migrations/MigrationRunner.cs:19-46`, `Server/DatabaseManager.cs:20-34`, `version.txt:1` `0.1.420` |
| P1-1 глобальный `Db.Lock` 36 мест | **Частично** — разделен `Db.cs:12-13` `GameLock`/`ContentLock` | Было `internal static readonly object Lock` → теперь два лока |
| P0-3 Editor писал в live `content.db` | **Закрыто** — Вариант А staging | `Editor.Core/Db.cs:14-46` `Live=content.db` `Content=content.editor.db`, `PublishToLive:85-135` `ATTACH+tx`, `FindDatabase:87-90` |
| P1-7/8 `DELETE FROM` + дрейф колонок | **Закрыто** — единый `ContentStore` | `Shared/Data/ContentStore.cs:23-283` `DeleteMissingRows` + `UpsertItem/Monster/Quest/Npc` + `UpdateNpcDialogue` |
| P1-9 `content.db` в git + неподписанный апдейт | **Закрыто** | `.gitignore:38-42` `*.db` + `!content.editor.db`, `build-client-build.ps1:136-203` `SHA256` сорт `147-171` инвариант |
| P2-9 версия commit-count | **Закрыто** | `version.ps1:15` монотон `version.txt`, `deploy-server.ps1:14` `Step-Version`, `installer.iss:9` override |
| `content.db` overwrite при деплое | **Закрыто** | `deploy-server.ps1:124-133` `game.db .bak` restore, `content.db` fresh |
| Storage Chest спека | **Подготовлена** | `NEXT_SESSION_STORAGE_CHEST.md:1-149` якоря `MapRenderer.cs:443`, `Chest_SS.png` 12 кадров |

---

## 2. Актуальные риски 22.08 (при CCU 2000 + freeze)

### P0 — критично, вайп/краш на старте, растет с данными

| ID | Где | Почему критично | Что будет если тянуть (CCU 2000) |
|---|---|---|---|
| P0-1a | `Shared/Migrations/MigrationRunner.cs:194-200` | `HasExistingTables` = `SELECT name='accounts'` → для `content.db` всегда `false` → свежий путь, destructive guard `26` не срабатывает. Любая порча `VersionInfo` в content.db = тихий ре-apply всех `CREATE TABLE`. | Чем больше контента — тем катастрофичнее двойной `CREATE`. Риск вшит в старт сервера. |
| P0-1b | `MigrationRunner.cs:121-128` `CleanStaleVersionInfo` | `DELETE FROM VersionInfo` стирает всю историю. Следующий старт `Repair:115` `MIN(Rowid) GROUP BY Version` криво (если `allVersions.Count<=1 return:109` пропустит дубликаты разных `AppliedOn`). | Репроверит все миграции, `VACUUM INTO` бэкап перезатрет. |
| P0-1c | `MigrationRunner.cs:141-150,165-177` | `stamp yyyyMMdd_HHmmss` коллизия в 1с (два старта), `RotateBackups` по `LastWriteTimeUtc` — clock skew mis-delete. | Бэкап перезатрет, keep 5 сломается. |
| P0-2 | `Server/GameWorld.cs:25,69-73` `HashSet<(int,int)> _obstacles` | `GameMap` без лока: `GameServices.cs:177,276` `ClearObstacles/AddObstacle` vs `GameServerHost.cs:74,91,106` `IsObstacle`/`FindPath` 50ms + `Program.cs:243-245` + `InteractionService.Tick:434-439` | Torn bucket `HashSet`, бесконечный цикл, краш процесса под нагрузкой. |
| P0-3 | `Server/Services/ZoneManager.cs:15-196` `Dictionary _portalLookup/_doors/_tiledNpcs` | `GameServices.cs:176` `ClearTiledRegistrations` мутирует пока `InteractionService.Tick:469` `FindPortal`/`Zones.GetAllTiledNpcs` читает | `InvalidOperationException` в тике движения, игрок застряет. |
| P0-4 | `Server/GameServices.cs:117-165` `ReloadContent/ReloadSectors` | Нет сериализации. Два `/reload` (консоль + чат `AdminCommands`) → `Clear+AddRange` на `_collectibleSpawns:51` пока `MonsterManager.Initialize:65` итерирует | Креш, `InventoryRepository.InvalidateTemplateCache:123` vs `GetTemplatesBatch:30` stale. |
| P0-5 | `Server/GameServices.cs:254-278` `RelocateStorage` | `RelocateStorage` не снимает старый `AddObstacle` → leak непроходимой клетки на каждый `/reload` | Со временем мир зарастает препятствиями вокруг мерчанта. |

### P1 — серьезно, стоимость рефактора растет с кодом (блокирует 2000 CCU)

| ID | Где | Риск | Если тянуть |
|---|---|---|---|
| P1-1 | `Server/Repositories/Db.cs:23-35` | Нет `PRAGMA journal_mode=WAL; busy_timeout=5000; synchronous=NORMAL; foreign_keys=ON`, нет `Cache=Shared/Pooling`. `CharacterRepository.SavePlayerProgress:155` `DELETE+INSERT` ~50ms/игрока. `PersistenceService.cs:17` unbounded `ConcurrentQueue<Player>` | При 2000 CCU `SQLITE_BUSY` (read vs write), queue растет → OOM, 100s drain. |
| P1-2 | `Server/DatabaseManager.cs:30-32` `File.Copy` | `File.Copy(editorContent→ContentPath)` без лока/атомарности. Конкурентный старт Editor+Server → half-copy → `malformed` DB. `CopyContentFromRuntimeIfNew:36` после миграции может перезатереть схему. | Битый `content.db` на старте. |
| P1-3 | `Server/Services/PersistenceService.cs:36-45,87` | `SaveBatch Dequeue → lock clear` гонка: `EnqueueSave` между хвостом и `clear` теряет имя → double-save. `FlushNow` та же гонка + `ProcessExit:269` 2s обрезка. | Потеря/дубль сейвов. |
| P1-4 | `Server/Services/DebuffManager.cs:46` | `async void TickDebuffs(Player)` — unawaitable, исключения крашат процесс. `GameServerHost.DebuffTick:195-203` не может `await`. Монстр-версия `:58` sync — инконсистентно. | Любой `await SendTargetPlayerDebuffUpdateAsync:27` exception = падение сервера. |
| P1-5 | `Server/Network/GameServer.cs:30-69,389` | `dirtyZones/entityDirty` `Keys.ToList()` + `TryRemove(zone)` теряет зоны, помеченные после snapshot (телепорт). `BroadcastMap 177-275` 6 dict+list аллокаций на тик → GC спайки. | Сталл позиций, лаг при 500+ CCU. |
| P1-6 | `Server/Network/GameServer.cs:769-810` `Program.cs:317-329` | `WriteLock.WaitAsync` без timeout → медленный мобильный холдит семафор навечно. `SendToAllAsync:787` sequential `await` vs `BroadcastMap:388` `Task.WhenAll` — чат 2000×5ms=10s. `GetConnectionCount>=Max:317` TOCTOU — лимит превышается, `Task.Run(HandleClientAsync)` unbounded → thread-pool starvation. | DoS, лаг чата. |
| P1-7 | `Server/Program.cs:264-273,660-679` | `ShutdownServer` double `FlushNow` + `Environment.Exit(0)` без `Serilog Flush`, `ConnectionGuard._lastSweep:25` без лока, `HandleClientAsync:342` `ReceiveTimeout` не работает с `NetworkStream.ReadAsync`. | Обрезанные сейвы, zombie-коннекты. |
| P1-8 | `Server/MessageHandlers/BuyHandler.cs:38` `EquipHandler.cs:37-57` | `price*qty int` overflow → отрицательный `totalCost` → фри-итемы. Без `TryReserveStock` oversell. `Equip` чистит `fromSlot=null` до `RequiredLevel/Blocked` → потеря предмета. | Дюп/потеря инвентаря. |
| P1-9 | `Server/GameWorld.cs:153-402` `Player` shared mutable | `Health` `MonsterAttackService:30` `Hazard:72` `CombatService:100` без `lock`/`Interlocked` + `Movement.Path` + `Inventory` + `Combat.*` — IO-thread vs unified loop | Двойной урон, отрицательный HP. |
| P1-10 | `Server/Services/AuthService.cs:138,152` `SessionManager.cs:13,87` | `GetConnectionByPlayerName` → `AddPlayer` не атомарно → дубль `Player`. `Validate` → `Revoke` TOCTOU → дубль сессии. `secret fallback dev-secret` → forge. | Дубль игроков, угон сессии. |
| P1-11 | `Server/Balance.cs:40` `MaxConnections=2000` | Высокий без `ThreadPool.SetMinThreads`, `GameServerHost` 50ms + `Heartbeat` 5s конкуренция. | Лог-спам, starvation. |
| P1-12 | `Client/Screens/GameScreen.cs:1-1479` | God 1479 строк, ~90 лямбд, `SubscriptionMatcher.cs:35-48` хрупкая эвристика leak-фикса (первый `field GetValue` пропускает мульти-delegate) | Новые фичи = новые лямбды → рост P0-2. |
| P1-13 | `Client/Rendering/MapRenderer.cs:75` `MinimapRenderer.cs:26` | 510 секторов preload + дубликат 30МБ в миникарте. `SpriteCache.cs:551` `Unload` только `_textures`, анимации/шрифты текут | RAM ×2, GPU leak logout. |

### P2 — умеренно, контролируемо (блокирует свободное добавление фич)

| ID | Где | Риск |
|---|---|---|
| P2-1 | `Shared/Models/Player.cs:5-60` facade 45 pass-through | 100 call-site к `Player.*`, новая стата = правка фасада + `ContentStore:163` + `ItemRoller:143` + Editor |
| P2-2 | `Shared/ErrorCodes.cs:9-32` `Network/GameMessage.Type` `SkillIds.cs:1` `ZoneIds.cs:10` `EquipmentSlots` | Строковые псевдо-enum → опечатка silent, нет compile-time |
| P2-3 | `Shared/Models/*` Anemic | `Health≤Max` `Quantity≤MaxStackForType (Balance.cs:176)` `DamageMin≤Max` не в модели |
| P2-4 | `Server/GameServices.cs:32-38 =null!` `Program.cs:180` `Pathfinding.cs:6` back-ref | Нельзя изолировать для тестов, новый сервис тянет статику |
| P2-5 | `Shared/Data/ContentStore.cs:32` `IN('...')` | Ручное экранирование `'`, SQL-инъекция имени ID, нет параметров |
| P2-6 | `Server/AccountRepository.cs:24-51` | Legacy SHA256 без соли, нет opportunistic rehash при логине |
| P2-7 | `Server/Network/GameServer.cs:113,164` `FindNpcAt` linear | `instance_portal` `FirstOrDefault` → не тот портал при N данжей, `FindNpcAt` скан на каждый `Interact` |
| P2-8 | `Server/Services/ZoneManager.cs:149` `GetOrCreateMap` fallback 100×100 | Игрок в fallback зоне видит обрезанную карту |
| P2-9 | `Server/Services/InteractionService.cs:377` `Tick` + `MoveToHandler:62` | `Path` без `lock`, `MoveInterval Math.Max(1.0,slow)` сумма слоу >10 → freeze 5s |
| P2-10 | `Build/deploy-server.ps1:94` `installer.iss:9` | Deploy под `root`, нет CI `.github/*`, `BackupBeforeMigration` stamp коллизия (P0-1c) |

---

## 3. Инварианты для новой модели (breaking-change разрешен)

* Разрешено мигрировать `Health`, `Level`, `Gold`, `Quantity` — старые сохранения конвертировать в `1032+` миграциях `ForwardOnlyMigration`. Seed-строки НЕ в миграции (дубль в обеих БД, `NEXT_SESSION_STORAGE_CHEST.md:92`).
* `MaxStackForType` `Balance.cs:176-179` `consumable/collectible/trophy/material →10 else 1` — валидировать в модели, не в хендлере.
* `version.txt:1` монотон, не commit-count.

---

## 4. Матрица техдолга (обновлена под 2000 CCU)

| Область | Уровень | Связи | Фаза |
|---|---|---|---|
| Миграции P0-1a/b/c | P0 | бэкап/деплой | 0 |
| GameMap obstacles race P0-2 | P0 | движение/поиск пути | 0 |
| ZoneManager Dictionary P0-3 | P0 | reload | 0 |
| Reload сериализация P0-4 | P0 | DI | 0 |
| WAL/queue P1-1/P1-3 | P1 | 2000 CCU | 1 |
| async void P1-4 | P1 | краш | 1 |
| dirty loss/broadcast P1-5 | P1 | 2000 CCU | 1 |
| WriteLock/лимит P1-6/7 | P1 | DoS | 1 |
| Player race P1-9 | P1 | бой | 1 |
| GameScreen god P1-12 | P1 | P0-2 | 4 |
| Sector preload P1-13 | P1 | RAM | 4 |
| SpriteCache leak | P1 | GPU | 4 |
| Anemic/enum P2-1..3 | P2 | расширяемость | 2 |
| DI статик P2-4 | P2 | тесты | 3 |
| ContentStore IN P2-5 | P2 | безопасность | 5 |

---

## 5. Приоритизированный бэклог по фазам (с freeze фич 0-1)

### Фаза 0 — Заморозка инвариантов (неделя 0-1) — БЛОКЕР для любых фич

**Цель:** сервер не вайпает, не крашит под reload.

- [ ] P0-1a: `MigrationRunner.HasExistingTables:194` → `SELECT count(*) FROM sqlite_master WHERE type='table' AND name NOT LIKE 'sqlite_%'`; для `content.db` проверять `items`/`monsters`, не `accounts`. Покрыть `MigrationRepairTests.cs`.
- [ ] P0-1b: удалить `CleanStaleVersionInfo:121` (`DELETE FROM VersionInfo`), в `RunMigrateUp:72` оставить только `RepairVersionInfo:91` + `throw` (не чистить соседей). `Repair` fix `if(allVersions.Count<=1)` → чекать `COUNT(*) != COUNT(DISTINCT Version)`.
- [ ] P0-1c: `BackupBeforeMigration:141` `stamp` → `yyyyMMdd_HHmmss_fff` + `Guid.NewGuid().ToString("N")[..4]`, `RotateBackups:168` по `FileName` (не `LastWriteTimeUtc`), keep 5 per `baseName`.
- [ ] P0-2: `GameWorld.cs:25` `lock(_obstacles)` в `AddObstacle/ClearObstacles/IsObstacle/GetObstacleData/Clone`. Альтернатива `ConcurrentDictionary`.
- [ ] P0-3 + P0-4: `ZoneManager.cs` → `ConcurrentDictionary` или `ReaderWriterLockSlim` для `_portalLookup/_doors/_tiledNpcs` + `GameServices.cs:117,165` `SemaphoreSlim(1,1) _reloadLock` вокруг `ReloadContent/ReloadSectors` + `SetSpawnData:48`.
- [ ] P0-5: `GameServices.RelocateStorage:254` хранить `prevStorage` и `World.Map.RemoveObstacle(prev)` перед `AddObstacle`; дубликат в `Program.cs:223` вынести в `GameServices.RelocateStorage` единый.

**DoD Фазы 0:** `DropAllTables` не вызывается без `LAD_ALLOW_DB_RESET=1`; `content.db` guard работает; 100× `/reload` + конкурентный `PublishToLive` без `InvalidOperation`/`malformed`; 2 конкурентных `/reload` не крашат.

### Фаза 1 — БД / сеть / безопасность 2000 CCU (недели 1-3, freeze фич)

- [ ] P1-1: `Repositories/Db.cs:23` `Open()/OpenContent()` → `PRAGMA journal_mode=WAL; busy_timeout=5000; synchronous=NORMAL; foreign_keys=ON; cache_size=-64000` + `ConnectionString` `Pooling=True`. `DatabaseManager.cs:30` убрать `File.Copy`, только `VACUUM INTO` под `ContentLock`.
- [ ] P1-1b: `CharacterRepository.SavePlayerProgress:155` батч `INSERT` + `PRAGMA query_only` для ридов, `InventoryRepository.GetForPlayer:192` `lock(GameLock)` → `ReaderWriterLockSlim`.
- [ ] P1-3: `PersistenceService.cs:17` → `Channel.CreateBounded<Player>(10000, DropOldest)` + `FlushNow:45` fix `clear` гонка (`Interlocked` + `TryRemove`).
- [ ] P1-4: `DebuffManager.cs:46` → `public Task TickDebuffs(Player)` + `await` в `GameServerHost.DebuffTick:195-203` + `try/catch` в `ApplyDebuff:26` fire-and-forget.
- [ ] P1-5: `GameServer.cs:32,217,389` dirty versioned: `ConcurrentDictionary<string,long> _dirtyVersion`, `MarkZoneDirty` `Interlocked.Increment`, `BroadcastMap` snapshot + `TryRemove(KeyValuePair)` только если версия совпала. Кэш `GroupBy` (monstersByZone etc) вне цикла, delta `EntityState` вместо полного `WorldMap`.
- [ ] P1-6: `GameServer.cs:774` `WaitAsync(TimeSpan.FromSeconds(5), ct)` + `Dispose` семафора в `World.RemoveClient:238` + `ClientConnection:14` `IDisposable`. `SendToAllAsync:787` → `Task.WhenAll` как `BroadcastMap:388`.
- [ ] P1-7: `Program.cs:317` → `World.TryAddClientWithLimit(Max, conn)` атомарно `lock(_lock)` + `Interlocked` counter + `SemaphoreSlim` для `Task.Run`.
- [ ] P1-8: `BuyHandler:38` `long total=checked((long)price*qty)` + `Merchant.TryReserveStock` под `lock`, `EquipHandler:37` валидация ДО `null`.
- [ ] P1-9: `Player` `object Sync = new()` + `lock(player.Sync)` для `Health/Mana/Inventory/Combat.Movement`, `Interlocked` для `Health` в `MonsterAttackService:30` `Hazard:72`.
- [ ] P1-10: `AuthService:138` `lock` на `AddPlayer` check-then-act, `SessionManager:87` `ValidateAndConsume` атомарно, `secret` без fallback, `parts` реконструкция payload безопасно.
- [ ] P1-11: `Balance.MaxConnections=2000` тюнинг `ThreadPool.SetMinThreads`, бэкап лимита в `GetMaxConnections()`.
- [ ] P2-6: `AccountRepository:51` opportunistic rehash SHA256→PBKDF2 при успешном логине.

**DoD Фазы 1:** WAL включен, `SQLITE_BUSY` 0 при 2000 фейк-коннектов, `BroadcastMap` <50ms, `async void` нет (`grep async void` 0 кроме `LoginScreen.cs:173` UI), лимит атомарен, `Buy` не overflow.

### Фаза 2 — Домен Player / типобезопасность (недели 3-5, breaking разрешен)

- [ ] `Facing.cs:12` уже enum — добить: `GameMessageType` enum (вместо строк), `ZoneId` typed (`ZoneIds.Main:10`), `SkillIds.cs:1` → `enum SkillId` + `SkillCatalog`, `ErrorCodes.cs:9` → enum.
- [ ] `Shared/Models/*` value-objects: `Health` (ctor `0..MaxHealth+Bonus`), `Quantity` (`<=MaxStackForType`), `EnhancementLevel`. Перенести `Player.GetCritChance` капы `MaxCritChance=75` внутрь.
- [ ] `Player.cs:5` facade → `[Obsolete]` pass-through, новые вызовы → `PlayerCombatStats`/`PlayerVitals`/`PlayerEquipment` компоненты. `ContentStore:163` колонки генерить из модели, не хардкод.
- [ ] `SubscriptionMatcher.cs:35` fix multi-delegate + depth limit + кэш `Enum.GetValues`.

**DoD Фазы 2:** `Facing/GameMessage/ZoneId/SkillIds` — типы; инварианты в моделях; новые фичи не через `Player.*` фасад.

### Фаза 3 — Сервер DI / ясность (недели 5-7)

- [ ] `GameServices.cs:32` `=null!` 9 сервисов → `required` + ctor `GameServices` собирает все (как `World/Hub/Sectors` уже). Убрать `Program:180` `SetServices` back-ref, `Pathfinding.Services` → явные зависимости `Configure:206`.
- [ ] Удалить мертвые `CombatService:97 RunCombatLoop` `HazardService:26 RunHazardTickLoop`, документировать `GameServerHost:34` unified loop.
- [ ] Покрыть сервисы unit-тестами через DI (сейчас 223 теста, добить `PlayerStats`, `QuestChain`).

**DoD Фазы 3:** `Program.Services` статик удален, все через `GameServices` ctor, мертвые loop удалены, сервисы мокаются.

### Фаза 4 — Клиент Windows (недели 7-10)

- [ ] `GameScreen.cs:1-1479` god → `SubscriptionMatcher` fix + method-group (или `IMediator`), `UnsubscribeAll` тест logout→login, `WindowManager` out.
- [ ] Вынести wiring в `GameScreenViewModel`/`Mediator` между `GameClient.cs:46` events и `Windows/*`.
- [ ] `MapRenderer.cs:75` 510 preload → viewport 3×3 стриминг, убрать `MinimapRenderer.cs:26` дубликат 30МБ (рисовать из `MapRenderer`), `Client/GameClient.cs:311` событие.
- [ ] `SpriteCache.cs:551` `Unload` диспозит анимации `Texture2D` + `SpriteFont`, `SpriteCache.cs:46` статик → `IDisposable` + eviction.
- [ ] `GameInputHandler.cs:185` унификация toggle, `NetworkManager.cs:290` порт из логина.

**DoD Фазы 4:** `UnsubscribeAll` 100% (тест), `GameScreen` <800 строк, RAM не O(площадь), GPU leak 0.

### Фаза 5 — Пайплайн / Editor (недели 10-13)

- [ ] `ContentStore.cs:32` `IN` → параметры `$p0..` + `whitelist table/pk`.
- [ ] `Editor.Core/Db.cs:85` `PublishToLive` уже `ATTACH+tx` + `VACUUM INTO backup:99` — добить `tx` параметризацию пути (`Replace("\\","/")` `ATTACH` инъекция `:108`), `ScanTiledMaps` без `bin/obj`.
- [ ] `build-client-build.ps1:184-199` подпись уже — добить `UpdateManager.cs:191` `TLS` pin + `manifest.sig` verify + `minVersion` floor (печать у игроков есть — можно форсить).
- [ ] `deploy-server.ps1:94` без `root` (User `lostanddivine`), rollback `.bak` для `content.db` тоже, не паковать локальный `game.db` `Server.csproj:21`.
- [ ] CI `.github/workflows/ci.yml` `dotnet test/build` + `DbBackups:45` ротация по `FileName`.

**DoD Фазы 5:** Editor изолирован от live, `content.db` вне git (уже), апдейты подписаны+TLS, общий репо Server+Editor, rollback при деплое, Editor-DB-drift тесты зеленые.

---

## 6. Следующая фича — Storage Chest (готова к старту после Фазы 0)

Спека `NEXT_SESSION_STORAGE_CHEST.md:1-149` проверена, P0-2 lock — блокер. Порядок из спеки:

1. Shared `WorldMap.StorageChest` `ChestPosition`, миграция `1032_AddPlayerStorage` `player_storage(player_name PK, items_json)` (ForwardOnly, `MigrationRunner` уже гейтит)
2. `Balance.StorageSlots=100` уже, `StorageService` + `GameServices.cs:41` поле
3. `Program.cs:223` + `GameServices.RelocateStorage:254` `AddObstacle` рядом с мерчантом
4. `Network/GameServer.cs:329` `StorageChest` для `main` в `BroadcastMap`
5. `SpriteCache` `chest_ss` `Chest_SS.png` 768×64 12×64 `idx0 storage closed 1 open 2-3 instance`, `MapRenderer:443,736,1313` spatial hash + курсор `talk` + `DrawStatic`
6. `Networking/ClientMessageHandlerRegistry.cs:14` `storage_open/update`, `StorageWindow` двухпанельный `Windows/ShopWindow.cs` паттерн + `GameInputHandler.cs:436` `OpenQuantity`
7. `dotnet build` + `dotnet test` 85/85 + ручной: сундук рядом с мерчантом, deposit/withdraw, рестарт — на месте

Чего НЕ делать: `MerchantWindow.cs` нет (есть `ShopWindow.cs`), кадры 4-11 не трогать, seed в миграцию не добавлять.

---

## 7. Верификация каждой фазы

```powershell
dotnet build LostAndDivine.Server
dotnet build LostAndDivine.ClientMonoGame
dotnet test LostAndDivine.Tests  # сейчас 85/85 зеленых
# Ручной CCU 2000: TestBot 2000× коннект, /reload 10×, рестарт — вещи на месте
```

## 8. Риски если не делать сейчас

Чем больше контента — тем дороже: P0-1 вайп катастрофичнее, P1-1 queue OOM при 2000, P1-12 god-object. При freeze 2-3 недели стоимость фикса сейчас минимальна, дальше — экспонента.

---

## 9. Вопросы закрыты 22.08

Все 6 вопросов отвечены → план выше уже с учетом 2000 CCU, breaking, freeze, staging, печать, Windows. Следующий шаг — Фаза 0.
